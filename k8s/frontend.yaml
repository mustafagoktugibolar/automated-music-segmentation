apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: music-segmentation
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: music-segmentation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: music-segmentation-frontend:latest
          ports:
            - containerPort: 80
