# This file is auto-generated from deployment.json. DO NOT EDIT MANUALLY.
services:
  music_segmentation_db:
    container_name: music_segmentation_db
    image: postgres:18-alpine
    ports:
      - ${DB_PORT}:${DB_PORT}
    volumes:
      - postgres_data:/var/lib/postgresql
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 5s
      timeout: 5s
      retries: 10
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      - music_segmentation_db

  music_segmentation_rabbitmq:
    container_name: music_segmentation_rabbitmq
    image: rabbitmq:3-management
    ports:
      - 5672:5672
      - 15672:15672
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test:
        - CMD
        - rabbitmq-diagnostics
        - -q
        - ping
      interval: 10s
      timeout: 5s
      retries: 5
  music_segmentation_backend:
    container_name: music_segmentation_backend
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./backend:/app/backend
      - ./shared:/app/shared
      - ./frontend:/app/frontend
      - ./logs:/app/logs
      - media_data:/app/media
      - ./data:/app/data
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: music_segmentation_backend
    depends_on:
      music_segmentation_db:
        condition: service_healthy
      music_segmentation_rabbitmq:
        condition: service_healthy
  worker-custom:
    scale: ${CUSTOM_WORKER_REPLICAS:-2}
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-custom
      WORKER_TYPE: custom_segmentation
      MESSAGE_CODE: segmentation.custom
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-2}
      OMP_NUM_THREADS: ${OMP_NUM_THREADS:-1}
      OPENBLAS_NUM_THREADS: ${OPENBLAS_NUM_THREADS:-2}
      MKL_NUM_THREADS: ${MKL_NUM_THREADS:-2}
      NUMEXPR_NUM_THREADS: ${NUMEXPR_NUM_THREADS:-2}
    depends_on:
      - music_segmentation_rabbitmq
  worker-msaf-foote:
    scale: 2
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-msaf-foote
      WORKER_TYPE: msaf_segmentation
      MESSAGE_CODE: segmentation.foote
      MSAF_ALGORITHM: foote
      WORKER_CONCURRENCY: 1
    depends_on:
      - music_segmentation_rabbitmq
  worker-msaf-cnmf:
    scale: 2
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-msaf-cnmf
      WORKER_TYPE: msaf_segmentation
      MESSAGE_CODE: segmentation.cnmf
      MSAF_ALGORITHM: cnmf
      WORKER_CONCURRENCY: 1
    depends_on:
      - music_segmentation_rabbitmq
  worker-msaf-scluster:
    scale: 2
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-msaf-scluster
      WORKER_TYPE: msaf_segmentation
      MESSAGE_CODE: segmentation.scluster
      MSAF_ALGORITHM: scluster
      WORKER_CONCURRENCY: 1
    depends_on:
      - music_segmentation_rabbitmq
  worker-fusion:
    scale: 2
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-fusion
      WORKER_TYPE: fusion_segmentation
      MESSAGE_CODE: segmentation.fusion
      WORKER_CONCURRENCY: 2
    depends_on:
      - music_segmentation_rabbitmq
  worker-dataset:
    container_name: worker-dataset
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-dataset
    depends_on:
      - music_segmentation_rabbitmq
    command: ["python", "workers/run_dataset.py"]
  worker-llm-1:
    container_name: worker-llm-1
    image: music-segmentation-worker:latest
    build:
      context: .
      dockerfile: workers/Dockerfile
    volumes:
      - ./workers:/app/workers
      - ./shared:/app/shared
      - ./logs:/app/logs
      - media_data:/app/media
      - ./data:/app/data
    env_file:
      - ./.env
    environment:
      SERVICE_NAME: worker-llm-1
      WORKER_TYPE: llm_segmentation
      MESSAGE_CODE: segmentation.llm
      LLM_PROVIDER: ${LLM_PROVIDER:-anthropic}
      LLM_MODEL_NAME: ${LLM_MODEL_NAME:-}
    depends_on:
      - music_segmentation_rabbitmq
  minio:
    container_name: minio
    image: minio/minio:latest
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/minio/health/ready || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 12
  minio_init:
    image: minio/mc
    depends_on:
      - minio
    entrypoint: /bin/sh -c
    command: |
      "sleep 5;
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD} || true;
      mc mb --ignore-existing local/${S3_BUCKET_RAW} || true;"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      S3_BUCKET_RAW: ${S3_BUCKET_RAW}
  salami_loader:
    container_name: salami_dataset_loader
    build:
      context: ../salami-dataset-loader
      dockerfile: Dockerfile
    env_file:
      - ./.env
    environment:
      S3_ENDPOINT: ${S3_ENDPOINT}
      S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      S3_SECRET_KEY: ${S3_SECRET_KEY}
      S3_BUCKET_RAW: ${S3_BUCKET_RAW}
      S3_REGION: ${S3_REGION}
      STORAGE_PROVIDER: minio
      DATASET_PREFIX: ${DATASET_PREFIX}
    depends_on:
      minio:
        condition: service_healthy
    volumes:
      - ./data:/app/data
    restart: "no"
    command: ["python", "main.py", "--concurrency", "5"]
  frontend:
    container_name: music_segmentation_frontend
    build:
      context: ./frontend/music-segmentation-ui
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:8000}
    ports:
      - "5173:80"
    depends_on:
      - music_segmentation_backend
volumes:
  postgres_data: null
  rabbitmq_data: null
  media_data: null
  minio_data: null
